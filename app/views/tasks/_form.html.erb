<% form_for @task do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
	<p>
		<%= f.label :project_id, "Project" %>
		<%= f.collection_select :project_id, @projects, :id, :name %>
	</p>
  <p><%= f.submit "Submit" %></p>
<% end %>
